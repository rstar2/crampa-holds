<div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				<p><a href="{{shopUrl}}">&larr; back to Shop</a></p>
				<hr>
				<header>
					<h1>{{product.title}}</h1>
				</header>
				<div class="post">
					{{#if page.image}}<img src="{{{cloudinaryUrl page.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{product.description.full.html}}}
				</div>

				<div class="post">
					Price is {{{product.price}}}
				</div>
			</article>
			<a href="?action=product.add&productId={{product.id}}">Add to cart</a>
		</div>
		<div>
			Cart : {{session.cart}}
		</div>
	</div>
</div>



