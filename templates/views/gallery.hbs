<div class="container-fluid">
	<div class="row">
		{{#each images}}
		<div class="col col-6 col-sm-4 col-md-3 gallery-image">
			<img src="{{imageUrl url width='300' height='300'}}" class="img-rounded">
		</div>
		{{/each}}
	</div>
</div>
